import React from 'react';
import { useTheme } from '../src/context/ThemeContext';

const ThemeSwitcher: React.FC = () => {
    // Hidden as per user request to keep site in light mode
    return null;
};

export default ThemeSwitcher;
