{
  "name": "barestask-static",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp -r App.tsx auth.tsx components dataStore.tsx index.html index.tsx metadata.json pages services types.ts dist/"
  }
}
